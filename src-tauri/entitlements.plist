<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow network access for AI API calls -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- Allow network server for local sidecar -->
    <key>com.apple.security.network.server</key>
    <true/>

    <!-- Allow file read/write access -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Allow microphone access for voice input -->
    <key>com.apple.security.device.audio-input</key>
    <true/>

    <!-- Disable library validation for loading external libraries -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Allow JIT compilation (needed for some WebView features) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Allow unsigned executable memory (needed for WebView) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Allow DYLD environment variables -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
</dict>
</plist>
